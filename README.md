# bridge-cloud-deploy

Configs + Dockerfiles for BRIDGE cloud services on Render.

| Service | Path | Purpose |
|---|---|---|
| gbrain-http | `gbrain-http/` | HTTP MCP server with OAuth 2.1 — phones + cloud agents reach the brain via HTTPS |
| (todo) openclaw | `openclaw/` | OpenClaw multi-agent runtime |
| (todo) clem | `clem/` | Clem (CRREM agent) — isolated service per CRREM data sensitivity |
| (todo) hermes | `hermes/` | Hermes (Railway, separate repo or here) |

See `~/.bridge-migration-2026-05-18/PHASE-*-COMPLETE.md` for full migration context.
